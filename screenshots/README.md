# Screenshots

Images referenced by the [main README](../README.md). Capture the plugin at the default 360×520 size against a light background for consistency.

| File | What to capture |
|---|---|
| `hero.png` | Plugin panel with 6–10 references in the grid, ideally alongside a moodboard on the Figma canvas |
| `search.png` | The `Results for "…"` banner active, showing AI search re-sorting the grid |
| `arrange.png` | Multi-select active (≥2 cards checked) with the Arrange dropdown open showing all seven layouts |
| `detail.png` | Detail drawer open on any reference — reactions row + tags + colors + comments + Extract → Tokens button |
| `settings.png` | Settings panel with the BYOK note visible |

PNG only. Keep widths under 1600 px so the README stays light to clone.
